#include "AnalyseDuFichier.h"
#define HEIGHT 700
#define WIDTH 900


int searchNode(node *tree,  int id,NOEUD* n);

int compare(const void *elem1, const void *elem2);

int affichage(way* ways,node* nodes,bounds b,int size);